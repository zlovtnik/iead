apiVersion: v1
kind: Service
metadata:
  name: {{ include "church-management.fullname" . }}
  labels:
    {{- include "church-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "church-management.selectorLabels" . | nindent 4 }}

